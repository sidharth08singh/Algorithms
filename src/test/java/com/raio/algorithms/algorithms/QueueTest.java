package com.raio.algorithms.algorithms;

import java.util.Arrays;
import org.testng.Assert;
import org.testng.annotations.Test;
import com.raio.algorithms.classes.Node;
import com.raio.algorithms.classes.QueueImpl;

public class QueueTest {
	/*@Test()
	public void testenqueue() throws Exception {
		QueueImpl q = new QueueImpl();
		q.enqueue(new Node(1, Arrays.asList(2,3,5), false));
		q.enqueue(new Node(2, Arrays.asList(1,3), false));
		q.enqueue(new Node(3, Arrays.asList(1,2,4), false));
		q.enqueue(new Node(4, Arrays.asList(3,5), false));
		q.enqueue(new Node(5, Arrays.asList(4), false));
		Assert.assertEquals(q.size(), 5);
	}
	
	@Test()
	public void testdequeue() throws Exception {
		QueueImpl q = new QueueImpl();
		q.enqueue(new Node(1, Arrays.asList(2,3,5), false));
		q.enqueue(new Node(2, Arrays.asList(1,3), false));
		q.enqueue(new Node(3, Arrays.asList(1,2,4), false));
		q.enqueue(new Node(4, Arrays.asList(3,5), false));
		q.enqueue(new Node(5, Arrays.asList(4), false));
		System.out.println(q.dequeue().getNodeId());
		System.out.println(q.dequeue().getNodeId());
		System.out.println(q.dequeue().getNodeId());
		System.out.println(q.dequeue().getNodeId());
		System.out.println(q.dequeue().getNodeId());
		Assert.assertEquals(q.size(), 0);
	}	*/
}
